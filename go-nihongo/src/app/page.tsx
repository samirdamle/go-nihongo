import { LookupWorkspace } from "@/features/lookup/lookup-workspace";

export default function Home() {
  return (
    <main className="flex flex-1 flex-col">
      <LookupWorkspace />
    </main>
  );
}
