# Sentence MT (pluggable — ADR 0004)
# Leave unset or set MT_PROVIDER=mock to use deterministic mock translator.
MT_PROVIDER=
GOOGLE_CLOUD_TRANSLATION_API_KEY=

# Optional later (not on silent dictionary path)
# GEMINI_API_KEY=
